version: "3.9"
services:
  python-server:
    build: .                     # Build from Dockerfile in current folder
    container_name: python-server
    restart: unless-stopped      # Auto-start on Pi reboot
    ports:
      - "8000:5000"             # Map host port 8000 → container port 5000
